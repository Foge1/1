package com.loaderapp.core.ui.theme

import kotlin.time.Duration
import kotlin.time.Duration.Companion.milliseconds

object AppMotion {
    val fast: Duration = 100.milliseconds
    val normal: Duration = 200.milliseconds
    val slow: Duration = 300.milliseconds
}
